# Database dependencies for SQLAlchemy and PostgreSQL integration
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL adapter for Python
alembic>=1.12.0         # Database migration tool

# Optional: for async support (if needed later)
# asyncpg>=0.29.0
# sqlalchemy[asyncio]>=2.0.0
